# This workflow will install Python dependencies, lint, format, and run tests with a single version of Python
# For more information see:
#    https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-python
#    https://docs.astral.sh/uv/guides/integration/github/

name: Python CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

permissions:
  contents: read

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v6

    - name: 🐍 Set up Python 
      uses: actions/setup-python@v6
      with:
        python-version-file: ".python-version"

    - name: 🟪 Install uv
      uses: astral-sh/setup-uv@v7
      with:
          version: "0.9.22"
          enable-cache: true
 
    - name: 🏗️ Install the project
      run: uv sync --locked --all-extras --dev

    - name: 🐕 Lint and Format
      run: |
        uv run ruff check
        uv run ruff format

    - name: 🧪 Run tests
      run: uv run pytest
